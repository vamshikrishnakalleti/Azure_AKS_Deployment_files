apiVersion: v1
kind: Service
metadata:
    name: vamshikrishnakalletiazureaksdeploymentfiles
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: vamshikrishnakalletiazureaksdeploymentfiles